version: 2
updates:
- package-ecosystem: pip
  directory: "/"
  schedule:
    interval: monthly
  open-pull-requests-limit: 10
  groups:
    pelican-dependencies:
      patterns:
      - '*'
      update-types:
      - "minor"
      - "patch"
